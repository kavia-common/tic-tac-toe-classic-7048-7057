This 'android' directory is a minimal stub for CI compatibility in an Expo app.

No real native Android project is generated by default. If your CI requires Gradle:

To generate a real native Android project:
1) cd ../
2) expo prebuild --platform android
3) cd android && ./gradlew assembleDebug

The current gradle wrapper scripts here are no-ops and will exit successfully.

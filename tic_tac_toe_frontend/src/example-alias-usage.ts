import { COLORS } from '@/theme';

// PUBLIC_INTERFACE
export function exampleGetPrimaryColor(): string {
  /** Returns the theme primary color; used to validate TS path alias configuration. */
  return COLORS.primary;
}
